{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "remote debug",
      "type": "shell",
      "command": "MAVEN_OPTS=\"${MAVEN_OPTS} -Xdebug -Xrunjdwp:transport=dt_socket,server=y,address=8090,suspend=n\"",
      "group": "build"
    }
  ]
}
